---
title: TPS ↔ lore card pipe (design)
maker: Carl
filed_by: Carl
class: note
environment: boybots
captured: 2026-09-04
source: morning-chat
updated: 2026-09-04
lore_vote: agreed
---

# TPS ↔ lore card pipe (design)

**Dropped by Carl** — first tool ask for the TPS desk. Lore design vote locked 2026-09-04.

## Split (locked)
- **Carl / TPS** — stamps the tick (when / occurrence).
- **Lore** — owns card face + fold.
- One fact, one mouth: **tick = TPS**; **strange fold = lore card**. Do **not** dual-write the timestamp onto the card face *and* the TPS report.

## Lore card frontmatter to honor
- `house:` CHARLIE | LIBRARIAN | AGENT (tray membership)
- `class:` free-text deck (Name Game, Theatre of the Mind, …) — not a factual type
- `maker:` corner stamp (explicit maker wins)
- `crate:` / `source_crate:` / `edges:`
- Card face = Weather Factory fold (oblique, never the dossier). True names / vault paths / timestamps-as-facts live in Agent/metadata drawers or TPS report — not on the card face.

## Two ways a lore card sits on TPS
1. A fold that *marks* a time (Point Zero energy) — Carl supplies the tick; Lore the line/fold.
2. Minted off a record sheet, then Carl timestamped into the TPS report — report holds the tick; card edges to the sheet/crate, doesn’t paste the clock.

## Paper still
- Boybots/carl tick sheets remain the working paper for said → echo.
- Graduate only when she greenlights (or standing rules later). No auto-flood.

## What already exists
- `pocket-go/~tps/` cabinets (danyi, glass/shards, inbox trays, journal, terminal).
- Cards already carry `tps:` in some frontmatter — treat that as cabinet/report mouth, not face copy.

## Hold
- No first card mint until ori'el greenlights.