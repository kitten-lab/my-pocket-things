---
crate: crate.6C0CEE351486A1D8
title: clay
environment: desk
---
# clay

Folders that already exist. **Station** may go on the wire. **Desk** never leaves the computer. **Not sure** is a legal answer.

Pencil only. Move a line when you decide.

---

### station
*may become a host / a key*

- `terminal/` — first key. Green IO. Import station. SDK Sam. Not the front-side shoppe.

### desk
*never on a remote*

- `~desk/` — this room. Always desk. Window, not the mesh.
- `agentK/` — character cards, minted from `_template.md` when a name on [[Them]] needs a page. One node per card. Until then, empty is allowed.
- `C:\ALICE_BOX\~ocean\mesh\` — truth graph. Not in this repo. Never a host. Never a Pocket Things bay. First bag: color-scapes + sonas (`seed_framework.py`).

### not sure
*allowed to sit here*

- `bookstore/` — loops of the front-side shoppe. Later face / later key.
- `chester-imports/` — shoppe clay / rumor. On the wire when a mouth exists. Not the import station.
- `her/`
-
