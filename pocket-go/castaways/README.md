This folder is for temporary backups of material that is partly malformed but likely to return to the pocket.

## parked · 2026-08-30

The whole prior `~library` vault is here as `~_shadow_~/~library/`.

Moved so the live pocket could be emptied and rebuilt around Go tokens and the shelves (librarian, tagger / charlie, readme). To bring a room back as a host, move that tree (or a piece of it) into `pocket-go/~hosts/{name}/`.
