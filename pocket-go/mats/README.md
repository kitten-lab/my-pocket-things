materials attached to the pocket. not a host. not the reader.

| folder | used by |
|---|---|
| `styles/` | `environment: name` → `name.css` |
| `imgs/` | `{{img:file}}` and wiki pictures |

later bags can sit beside these (fonts, audio, whatever). fetch a repo into here when that exists. until then this is the shelf.

reader chrome (`www.css`, the explorer dress) stays in `prod/www_sys/`. skins and pictures do not.
