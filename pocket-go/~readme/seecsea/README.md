# C looking (room letter)

The joke is the paper. These slips are the walk. Do not paint the rhyme.

Walk started 2026-09-01. First pass was only the recent surface. This is the sort: **point at C:, do not swallow it.**

## How we sort (Go, not a fifth house)

Five looking-slips under `go.seecsea/` — they show in `{{dir}}`:

| slip | covers |
|---|---|
| island | live ALICE_BOX companies + Reborn + Bone Collector |
| bags | 893 cores, glass-logs, forage transcripts |
| vaults | named Obsidian rooms in `_VAULTS` — a folder of vault pages |
| silos | Builds product trees |
| attic | mausoleum, forage, museum, pre-agent, mesh, ~retired |

Disk stays. Gold stays gold. Corpses stay labeled. Artifacts in Go are slips, then later a faxed room when a pile earns one.

## Counts this walk (files, not completeness)

C:\ work islands: ALICE_BOX · ALICE_REBORN · BONE COLLECTOR · Builds. `C:\New folder` is empty.

Biggest holes still unwalked inside a pile:

- `_VAULTS/Redacted` (5968) — do not dump
- `__PRE-AGENT-REPOS-kitten-lab` (24835) — attic, skip unless Hands names a folder
- `my-pocket-internet` (7758) and `silo-the-forgetting-house` (5728) — silo, one README each next
- `_VAULTS/The Old Realms/Poetry` (396) — next child inside the island; do not dump
- Terminal IO vault (681) — dress already faxed; the vault itself is still a look if Hands wants it

## Already looking (do not redo)

- Book of Hours librarian notes → `go.journal`
- Terminal IO dress → `go.terminal/IO`
- Old Realms island letter + Memoir child → `go.seecsea/vaults/old-realms`
- DANYI.COM Youth writings → `go.danyi`
- Charlie's Toys · Eddy's Encoder → `go.seecsea/island/eddies-encoder`

## Next walk (one folder)

Open a slip. Walk that pile’s own README, then one child. Tell back. Then a smaller Go note if it earns one.

Island is now a folder of looking-slips. Next ROM on Charlie's Toys if Hands names one; else Poetry (396) inside Old Realms.
